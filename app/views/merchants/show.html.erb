<body>
  <header>
    <h1>Merchant <%= @facade.merchant_id %></h1>
  </header>
    
  <section class="items">
    <% @facade.merchant_items.each do |item| %>
      <div id="item-<%= item.id %>">
        <%= link_to item.name, item_path(item.id) %>
      </div>
    <% end %>
  </section>
</body>